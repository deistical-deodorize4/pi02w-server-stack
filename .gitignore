# Environment files (may contain real secrets)
.env
.env.local
.env.*.local

# Private keys and certificates
*.pem
*.key
*.pfx
*.p12
id_rsa
id_rsa.pub

# FileBrowser runtime data
files/
filebrowser/

# Credential files
credentials
.secret*
